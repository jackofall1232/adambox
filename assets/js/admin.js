/* AdamBox Admin JS
 * File: assets/js/adambox-admin.js
 * Purpose: Admin-side behavior (intentionally minimal)
 */

(function () {
	'use strict';

	// DOM ready
	document.addEventListener('DOMContentLoaded', function () {
		const adminWrap = document.querySelector('.adambox-admin');
		if (!adminWrap) {
			return;
		}

		// Placeholder for future admin interactions.
		// Keeping this file intentionally light to avoid bloat
		// and ensure WordPress.org compliance.

		// Example safe hook (currently unused):
		// console.debug('AdamBox admin loaded');
	});
})();
